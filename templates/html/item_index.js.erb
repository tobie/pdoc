if (!window.PDoc) window.PDoc = {};
PDoc.elements = {
  <%= @root.registry.map do |k, v|
    "'#{k}': { 'name': '#{k}', 'type': '#{v.type}', 'path': '#{ raw_path_to(v).join('/') }' }"
  end.join(",\n") %>
};